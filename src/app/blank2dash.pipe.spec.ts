import { Blank2dashPipe } from './blank2dash.pipe';

describe('Blank2dashPipe', () => {
  it('create an instance', () => {
    const pipe = new Blank2dashPipe();
    expect(pipe).toBeTruthy();
  });
});
